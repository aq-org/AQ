// Test Issue #2: Multiple folders with same-named files should resolve correctly
// ../common.aq from different folders should not conflict
import "folder2/sub_module.aq" f2;
import "folder3/sub_module.aq" f3;

int main() {
    __builtin_print("Test Issue #2: Path collision resolution\n");
    
    int f2_parent = f2.get_parent_id();
    __builtin_print("Folder2's parent ID (should be 1): ");
    __builtin_print(f2_parent);
    __builtin_print("\n");
    
    int f3_parent = f3.get_parent_id();
    __builtin_print("Folder3's parent ID (should be 2): ");
    __builtin_print(f3_parent);
    __builtin_print("\n");
    
    if (f2_parent == 1 && f3_parent == 2) {
        __builtin_print("Path resolution test PASSED!\n");
    }
    
    return 0;
}
